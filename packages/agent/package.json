{
  "name": "daopai-agent",
  "version": "0.1.0",
  "private": true,
  "description": "DaoPai 本地执行端 — 安装在本机执行浏览器自动化任务",
  "scripts": {
    "dev": "tsx src/index.ts",
    "build": "tsc -p tsconfig.json",
    "start": "node dist/index.js"
  },
  "dependencies": {
    "axios": "^1.6.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "tsx": "^4.0.0",
    "typescript": "^5.6.0"
  }
}